let tinggiSegitiga = 10; // Tinggi segitiga siku-siku

// Loop dari baris 1 hingga tinggi segitiga
for (let baris = 1; baris <= tinggiSegitiga; baris++) {
  // Cetak '*' sebanyak nilai baris saat ini
  console.log('*'.repeat(baris));
}
